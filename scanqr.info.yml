name: 'Scan QR'
description: 'A custom module for QR code scanning functionality with field support'
type: module
core_version_requirement: ^11
package: Custom

# Dependencies
dependencies:
  - drupal:field
  - drupal:file
  - drupal:image

# Optional dependencies
suggests:
  - webform:webform

# Configuration
configure: scanqr.settings